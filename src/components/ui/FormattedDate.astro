---
type Props = {
  date: Date
}

const { date } = Astro.props
---

<time datetime={date.toISOString()}>
  <strong>
    {
      date.toLocaleDateString(undefined, {
        dateStyle: 'long',
      })
    }
  </strong>
</time>
